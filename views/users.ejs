<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>USERS PAGE</title>
  </head>
  <body>
    <h1>USERS</h1>

    <table>
     <thead>
       <tr>
         <td>First Name</td>
         <td>Last Name</td>
         <td>Email</td>
         <td>Age</td>
       </tr>
     </thead>
     <tbody>
        <% for(let i = 0; i < userResults.length; i++){ %>
          <tr>
            <td><a href='/profile/<%= userResults[i].id%>'><%=userResults[i].first_name %></a></td>
            <td><%=userResults[i].last_name %></td>
            <td><%=userResults[i].email%></td>
            <td><%=userResults[i].age %></td>
          </tr>
        <% }; %>

     </tbody>
   </table>

  </body>
</html>
